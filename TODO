- tcc fails:
  - mingw_cross: 129_scopes
  - mingw: 129_scopes 101_cleanup 119_random_stuff
  - darwin: 106_versym 108_constructor 119_random_stuff 120_alias
            124_atomic_counter 128_run_atexit 140_arm64_extasm
- torture fails:
  - mingw_cross: 20030613-1 20080222-1 built-in-setjmp conversion fprintf-chk-1
  - darwin: 20000731-1 20011217-1 20040313-1 20080222-1 930513-1
    fprintf-2 fprintf-chk-1 loop-2f loop-2g
- run the peephole checks directly in the codegen pass.
  it only needs to checks against the previously emitted line.
- emit asm bytes/.o files directly, no need for GAS, we only need the linker then.
  the linker can be mold or gold. this saves a ton of space.
- add -O2 passes, like -finline and -funroll, but no need for SSA analysis.
- for the CTFE check, set more pure attributes to known library function calls,
  which are pure in the given arguments. observe the purity of calls in CTFE
- optional QBE backend for -O2
